{
  "name": "texture-forge",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types scripts/test-engine.ts",
    "render": "node --experimental-strip-types src/cli.ts",
    "check:browser": "node scripts/browser-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.7.2",
    "vite": "^5.4.10",
    "playwright": "^1.48.0"
  }
}
